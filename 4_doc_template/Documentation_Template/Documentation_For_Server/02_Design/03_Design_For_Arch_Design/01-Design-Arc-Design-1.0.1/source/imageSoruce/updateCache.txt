@startuml
 (*) --> "缓存系统"
 if "是否有数据" then
  -->  ===B1===    
  -->[true]"获取数据"
  --> ===B2===    
  --> "返回数据"
 else 
  ===B1=== -->[（false 或 缓存数据过期）] "加载缓存"
  -->"更新缓存"
  -->"更新缓存文件"
  --> ===B2===    
 endif  
  
 
 title 被动更新
 
@enduml