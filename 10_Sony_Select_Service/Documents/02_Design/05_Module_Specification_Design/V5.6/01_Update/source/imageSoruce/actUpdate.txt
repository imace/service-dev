@startuml

:客户端调用接口app.getLatestVersions;

if (输入参数校验?) then (校验不通过)

  :返回 参数错误;

else (校验通过)

  if (应用列表缓存?) then (已经缓存)

    :从缓存中读取应用列表;
  
  else (尚未缓存)
    :从数据库读取应用列表";
    :添加应用列表到缓存;
  endif

  :匹配机型、包名;
  :匹配最大版本;
  :返回 最新应用列表;

endif

stop

@enduml