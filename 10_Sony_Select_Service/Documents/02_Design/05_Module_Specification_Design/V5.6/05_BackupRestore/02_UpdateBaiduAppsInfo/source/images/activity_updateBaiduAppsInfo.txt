@startuml

start

:按时启动更新服务;
:清除保存的百度应用信息;
:按下载频率获取备份应用列表;
if(成功获取) then (yes)
	:获取百度应用的排序列表;
	:对百度列表进行分批;
	:单批获取最新的百度应用信息;
	if(成功获取百度应用信息) then(yes)
		:保存最新的百度应用信息;
	else(no)
		:记录失败的记录数;
	endif
	
else(no)
	

endif
:输出更新结果;
stop

@enduml